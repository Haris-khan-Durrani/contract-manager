# =============================================================================
# CONTRACT MANAGEMENT SYSTEM — ENVIRONMENT CONFIGURATION (TEMPLATE)
# Database (MySQL 8+) connection settings ONLY.
# All other application & integration settings are stored in MySQL (system_settings).
# =============================================================================

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=contractmanager
DB_USER=root
DB_PASSWORD=

PORT=3001
NODE_ENV=development
